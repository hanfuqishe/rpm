# 同构双向递归模型：对 OSI 参考模型的重构与推广

**Isomorphic Bidirectional Recursive Model: A Reconstruction and Generalization of the OSI Reference Model**

**摘要**：OSI 七层参考模型自提出以来已成为网络体系结构教学与描述的基本框架，但其“分层 + 每层对等通信”的静态划分在面对现实协议体系（如 IP 承载于多种链路、光网络多层复用、运营商级以太网控制面）时暴露出若干不足：层与层之间职责划分模糊、路由协议归属混乱、介质层以下缺少统一的递归描述。本文提出**同构双向递归模型**（Isomorphic Bidirectional Recursive Model, IBRM）：该模型断言**每一个中间层协议都拥有相同的四层内部结构**（承载层、数据链路层、网络层、传输层），四层结构在所有协议层中递归嵌套、完全同构；向下递归形成“承载链”（每一层以下层为链路），向上递归形成“传输链”（每一层以上层为载荷），并以上数据层（不再封装）与下介质层（不再承载）作为递归终止边界。模型进一步对“网络层”做纵向分割，将其划分为**转发面**（对外服务，逐包高速搬移）与**控制面**（对内信令，生成并维护转发依据）。文章以 TCP、IP、以太网、编码调制、光波/光纤等层为例，系统给出了各层控制面与转发面的具体实例，并指出 BGP/RIP 应归为 IP 控制面信令而非应用层、TTL 属于 IP 转发面、光放大器整形与否决定其层次归属等若干在 OSI 框架下难以自洽、而本文模型可自然推导的结论。该模型为网络体系结构的教学、协议分类与光—电协同网络的统一描述提供了一种更一般化的框架。

**关键词**：同构双向递归模型；递归分层；控制面；转发面；OSI 模型；GMPLS；波分复用

---

## 1 引言

OSI（Open Systems Interconnection）七层参考模型将网络体系划分为物理层、数据链路层、网络层、传输层、会话层、表示层与应用层，并假定每一层向上层提供服务、向下层使用服务、对等层通过协议通信。OSI 模型在教育与协议描述中取得了巨大成功，但现实网络协议的演进已经超出了该框架的表达能力：

- **层归属混乱**：BGP 通过 TCP 承载、RIP 通过 UDP 承载，OSI 因而将其归为“应用层”；但 BGP/RIP 的功能本质是维护 IP 的路由/转发表，并不对外提供用户服务。按 OSI 的分类，同一功能（路由协议）因“被谁承载”而得到不同的层归属，缺乏一致性。
- **介质层以下缺乏递归描述**：光纤上存在波分复用（WDM），同一根光纤可承载数十上百个波长；OSI 把这一切全部压入“物理层”，无法表达“波长”这一具有独立寻址与转发语义的层次。
- **控制面/转发面缺失**：现代网络的本质特征是“控制面（决策）与转发面（搬移）分离”，但 OSI 模型没有为这一纵向维度提供位置。
- **“层”的定义不统一**：以太网的“网桥转发”、IP 的“路由转发”、TCP 的“端口到进程交付”、光层的“波长交换”在语义上都属于“在不直接相邻的节点间转发数据”，但 OSI 把它们的层次归属与内部机制完全割裂。

本文提出的同构双向递归模型（IBRM）试图回答这样一个问题：**是否存在一个统一的递归结构，使得“每一层协议”都能被同一种方式描述？** 我们的答案是肯定的——每一个中间层协议自身都拥有相同的四层结构（承载层、数据链路层、网络层、传输层），该结构在所有协议层中递归嵌套。在此基础上，网络层进一步纵向分割为转发面与控制面。

## 2 模型总览

### 2.1 三个核心命题

同构双向递归模型由三个命题构成：

**命题一（同构）**：每一个中间层协议都拥有相同的四层内部结构——承载层、数据链路层、网络层、传输层。即：
- **传输层**：把上层协议整体视为自身载荷（向上递归的方向）；
- **网络层**：负责在不直接相邻的“节点”之间转发数据（本协议的功能核心）；
- **数据链路层**：向下层发送数据时，将下层视为透明承载，认为数据被交给了**相同协议的对等节点**——即通过承载层一跳到达对端的同层实体，实现“直接相邻节点间”的对等交付（而非与“下一层节点”直接交互）；
- **承载层**：把下层协议整体视为一条链路（向下递归的方向）。

**命题二（双向递归）**：
- **向下递归形成承载链**：每一层以下层为承载链路，即“承载层 = 下层协议”；
- **向上递归形成传输链**：每一层以上层为载荷，即“传输层 = 上层协议”。

**命题三（边界条件）**：
- **向上终止于数据层**：最上层不再封装，无内部层次，是传输链的终点；
- **向下终止于介质层**：最下层不再承载，是承载链的终点。

### 2.2 模型的递归性

我们以一个运行于波分系统上的HTTP协议栈为例：由命题一至三，一个协议栈可以看作一条由“承载链”组成的自顶向下的递归链，和一个由“传输链”组成的自底向上的递归链：

```
数据层（原始数据，递归终点）
   ↑ ← 传输链（自底向上：上层为载荷）
   ↓ ← 承载链（自顶向下：下层为承载）
HTTP 层
   ↑
   ↓
TCP 层
   ↑
   ↓
IP 层
   ↑
   ↓
以太网层
   ↑
   ↓
编码调制层
   ↑
   ↓
光波层
   ↑
   ↓
光纤层（物理介质，递归终点）
```

除两个终点，其中每一层都以相同的四层结构展开：以“下层为承载、上层为载荷”，在自身内部完成两种交付分工——**数据链路层**把下层视为透明承载，向**相同协议的对等节点**做“一跳”对等交付（即“直接相邻节点间”通信）；**网络层**则在**不直接相邻**的节点之间做多跳转发。

## 3 网络层的纵向分割：转发面与控制面

在递归四层结构的基础上，IBRM 进一步对“网络层”做纵向分割：

**网络层 = 转发面（Forwarding Plane）+ 控制面（Control Plane）**

- **转发面**：实际执行数据包从入接口到出接口的搬移（查表、搬包、封装/解封装）。特征是逐包处理、实时、高速，**对外服务**，承载用户数据。
- **控制面**：生成并维护转发面所需的转发依据（路由表、转发表）。特征是事件驱动（收敛/变化时）、相对低速，**对内服务**，不对外提供用户数据传输。

在可视化展示中，控制面采用蓝/紫标识（强调“对内信令”），转发面采用绿色标识（强调“对外服务”），并在控制面内部进一步用颜色区分**静态**（如静态路由、人工配置）与**动态**（如 OSPF/BGP 等动态路由协议、GMPLS）机制。

## 4 关键结论：若干在 OSI 框架下难以自洽、而本文模型可自然推导的命题

### 4.1 路由协议的归属

在 IBRM 中：
- 路由协议（OSPF、BGP、IS-IS、RIP 等）**不属于传输层**（它们不承载用户数据）；
- 路由协议**不属于应用层**（它们不对外提供用户服务）；
- 路由协议 = **IP 协议自身的控制面信令**，服务于 IP 的转发决策，类似于 ICMP 是 IP 的运维面信令。

**与 OSI 模型的区别**：OSI 因 BGP 使用 TCP 承载、RIP 使用 UDP 承载而将其归类为“应用层协议”。在 IBRM 中，承载层与被承载的协议层并无绑定关系，“被谁承载”不决定它“属于哪一层”。BGP/RIP 的承载层是 TCP/UDP/IP，但其功能本质是 IP 的控制面，而非应用层。由此，“静态路由”（人工配置）与 OSPF/BGP/IS-IS/RIP（动态协议）共同构成 IP 网络层控制面的静态/动态两极。

### 4.2 TTL 的定位

TTL（Time to Live）归属于 IP 网络层的**转发面**：每一跳路由器将 TTL 减 1，当 TTL 变为 0 时丢弃该包并返回 ICMP 超时报文。TTL 是在转发路径上执行的、影响转发决策（丢弃或继续转发）的核心机制；TTL 超时生成 ICMP 超时报文，是“转发面触发运维面”的一个典型例子。控制面（路由协议）不使用 TTL 做决策。

### 4.3 光放大器整形与否决定层次归属

介质层的分解由物理介质的实际复用能力决定。以光纤为例，介质层可分解为**光波层 + 光纤层**。而光放大器是否带整形，决定了它属于哪一层：
- **不带整形**（如 EDFA，仅做纯物理放大）→ 属于**光波层转发面**（只涉及光的物理特性：波长、功率、方向，不涉及“数字↔物理信号”映射）；
- **带整形**（如 SOA 的 3R 再生，涉及解调 + 再调制）→ 属于**编码调制层转发面**（涉及“物理信号→数字→物理信号”转换）。

### 4.4 GMPLS 的归属

GMPLS（通用多协议标签交换）控制的是**波长通道的建立与调度**，属于**光波层控制面**；其控制面协议（OSPF-TE + RSVP-TE）运行在 IP 层之上，但控制的是光层设备（ROADM/OXC/OTN），实现 IP 层控制面与光层控制面的联动，使光路建立从“人工配置”变为“动态建立”。在 IBRM 中，GMPLS 是光波层控制面的动态部分，与静态的“波长通道选择（数据流↔波长映射）”共同构成光波层完整控制面。

### 4.5 NAT 与 TCP 网络层的同构性

TCP 在网络层通过端口号将 IP 提供的点到点链路复用后，在同一主机内部将数据流交付给不同的进程（端口 → 进程映射、Socket 查表交付）。NAT 将这一“端口到进程”的交付逻辑扩展到网络边界：外部 (IP:Port) → 内部 (IP:Port) → 进程。两者本质相同——都是“查表 + 交付”，只是查表的范围与位置不同（主机内部 vs 跨主机边界）。

## 5 各协议层的分解实例

我们仍以运行于波分系统上的HTTP协议栈为例，逐层给出 IBRM 框架下各协议层的展开内容（与配套交互式可视化 IBRM.html 一致）。需要说明的是：各层**数据链路层**的具体实现各不相同（如 TCP 层的校验和/重传、以太网层的 CRC/碰撞检测、编码调制层的数字↔物理信号映射、光纤层的物理介质标识），但其本质角色一致——**把下层视为透明承载，向相同协议的对等节点做“一跳”对等交付**；而各层**网络层**则在不直接相邻的节点之间做多跳转发。

### 5.1 数据层（传输链递归终点）

扁平、无内部结构，是传输链的终点（不再封装）。

### 5.2 HTTP 层

- **网络层**：空（HTTP 不做非直接相邻的转发决策）。
- **数据链路层**：URL/方法、状态码（传输数据，不理解内容）。

### 5.3 TCP 层

- **网络层**：根据端口号查表，将数据交付给正确的接收者。
  - **控制面**：
    - 本机模式：端口绑定管理（bind/listen/accept）；
    - 网络模式（NAT）：NAT 映射规则管理（外部端口 ↔ 内部主机+端口）。
  - **转发面**：
    - 本机模式：根据端口查表 → 交付给 Socket FD；
    - 网络模式（NAT）：查 NAT 映射表 → 转发到内部主机。
- **数据链路层**：连接状态管理（SYN/EST/FIN）、校验和、重传、流控、端口复用。

### 5.4 IP 层

- **网络层**：
  - **控制面**（对内信令）：静态路由；动态路由协议 OSPF、BGP、IS-IS、RIP。
  - **转发面**（对外服务）：查表、搬包、封装解封装、TTL 处理（每跳减 1，超时丢弃）。
- **数据链路层**：分片重组、协议号复用、头部校验。

### 5.5 以太网层

- **网络层**：
  - **控制面**（对内信令）：MAC 地址学习（生成转发依据）、PBB-TE (802.1Qay)、G.8080 ASON、MAC-in-MAC + 控制平面、地址空间分割。其中 PBB-TE 禁用传统 MAC 学习与生成树，通过控制面静态/半静态配置转发路径，使以太网具备流量工程能力；ASON 将自动交换光网络的控制平面架构应用于以太网交换装置；MAC-in-MAC 通过控制面直接配置转发表，用户 MAC 在核心网络不可见；地址空间分割将 48 位 MAC 空间划分为无连接与面向连接两个区域。
  - **转发面**（对外服务）：根据 MAC 地址查表转发、泛洪、过滤。
- **数据链路层**：CRC 校验、碰撞检测（MAC 寻址是转发决策依据，属网络层功能而非链路层功能）。

### 5.6 编码调制层

- **网络层**：
  - **控制面**：空（不涉及转发路径决策——调制格式、FEC、功率等属于“数字↔物理信号”的物理适配，不参与转发路径决策）。
  - **转发面**：中继器、带整形的光放大器（如 SOA 3R 再生）、波长转换器、带 O-E-O 转换的 ROADM/OTN（涉及解调 + 再调制）。
- **数据链路层**：数字 ↔ 物理信号映射、调制格式选择（PAM4/NRZ）、FEC 配置、功率设置、波长调制参数配置（中心频率/调制格式/FEC/功率）。

### 5.7 光波层（介质层分解：光纤场景）

介质层的层次结构不是预设的，而是由物理介质的实际能力决定的。光纤支持波分复用（WDM），因此介质层可分解为“光波层 + 光纤层”。

- **网络层**：
  - **控制面**：波长通道选择（数据流↔波长映射）、GMPLS（OSPF-TE/RSVP-TE，动态建立光路）。
  - **转发面**：ROADM 波长交换、光放大器（不带整形，如 EDFA）、GMPLS 动态光路。
- **数据链路层**：波长寻址（λ₁, λ₂, …, λₙ）。

### 5.8 光纤层（承载链递归终点）

- **网络层**：
  - **控制面**：光缆施工规划、ODF 配线架管理。
  - **转发面**：光纤熔接、机械接续、ODF 跳纤。
- **数据链路层**：光纤（物理介质本身）。
- **承载层**：无（递归终点）。

## 6 与 OSI 模型的比较

| 维度 | OSI 七层模型 | 同构双向递归模型（IBRM） |
|:---|:---|:---|
| 基本结构 | 全局固定 7 层，每层职责唯一 | 每一层协议内部递归包含相同的 4 层结构 |
| 递归性 | 无（层次一次划分） | 双向递归（承载链向下、传输链向上），两个终止条件 |
| 层归属规则 | 由“被谁承载”大致决定层次 | “被谁承载”只决定承载层是谁，不决定所属层次 |
| 路由协议归属 | BGP/RIP 归为应用层（因其承载于 TCP/UDP） | BGP/RIP 归为 IP 网络层控制面信令 |
| 控制面/转发面 | 无此纵向维度 | 网络层 = 控制面（对内信令）+ 转发面（对外服务） |
| 介质层描述 | 全部压入物理层 | 由复用能力决定是否分解（光纤→光波层+光纤层） |
| 对新技术的容纳 | 需要新增或调整层定义 | 统一以“某层 + 四层递归 + 控制/转发面”描述 |

## 7 讨论

**（1）控制面的普适性。** 控制面不是 IP 层的特权，而是任何具有“转发”功能的层次都可以拥有的内部决策机制，其复杂度可以从极高（IP 层的 OSPF/BGP）到极低（光纤层的施工规划）不等。这验证了模型的核心命题：控制面/转发面的纵向分割具有跨层普适性。

**（2）静态与动态控制面的统一。** 各层控制面均包含“静态/半静态”与“动态”两类机制：IP 层（静态路由 vs OSPF/BGP）、以太网层（PBB-TE/MAC-in-MAC/地址空间分割 vs ASON/G.8080、MAC 学习）、光波层（波长通道选择 vs GMPLS）。

**（3）转发面触发运维面。** TTL 超时 → 生成 ICMP 超时报文，是“转发面触发运维面”的实例；类似地，以太网 OAM（IEEE 802.3ah / 802.1ag）属于以太网层的运维面，类比 IP 层的 ICMP。

**（4）可视化验证。** 配套的交互式页面（IBRM.html）将上述八层协议以可展开协议栈呈现：悬停任意层展开其内部“网络层（控制面/转发面 2×2 表格）+ 数据链路层”，右侧标尺动态展示该协议视角下的“传输层/网络层/数据链路层/承载层”四分结构，验证了模型的一致性、自洽性与教学可用性。

## 8 结论

本文提出了同构双向递归模型（IBRM）。该模型以“每一层协议拥有相同的四层结构”为同构公理，以“向下承载、向上传输”为双向递归机制，以数据层与介质层为递归边界；在此基础上，将网络层纵向划分为转发面与控制面。模型能够自洽地解释路由协议的层次归属、TTL 的转发面属性、光放大器整形与否的层次归属、GMPLS 的光波层控制面属性、NAT 与 TCP 网络层的同构性，以及介质层随复用能力而分解等 OSI 框架难以统一描述的现象。IBRM 为网络体系结构的教学、协议分类、以及光—电协同网络的统一建模提供了一个更具一般性的递归框架。

## 参考文献

[1] International Organization for Standardization. ISO/IEC 7498-1: Information technology — Open Systems Interconnection — Basic Reference Model: The Basic Model[S]. 1994.

[2] ITU-T. G.8080/Y.1304: Architecture for the automatically switched optical network (ASON)[S]. 2006.

[3] IEEE. 802.1Qay-2009: Virtual Bridged Local Area Networks — Amendment: Provider Backbone Bridge Traffic Engineering[S]. 2009.

[4] IEEE. 802.1ah-2008: Provider Backbone Bridges (MAC-in-MAC)[S]. 2008.

[5] Kompella K, Rekhter Y. OSPF Extensions in Support of Generalized Multi-Protocol Label Switching (GMPLS): RFC 4203[S]. IETF, 2005.

[6] Berger L. Generalized Multi-Protocol Label Switching (GMPLS) Signaling Resource ReserVation Protocol-Traffic Engineering (RSVP-TE) Extensions: RFC 3473[S]. IETF, 2003.

[7] Rekhter Y, Li T, Hares S. A Border Gateway Protocol 4 (BGP-4): RFC 4271[S]. IETF, 2006.

[8] Malkin G. RIP Version 2: RFC 2453[S]. IETF, 1998.

[9] Postel J. Internet Control Message Protocol: RFC 792[S]. IETF, 1981.

[10] IEEE. 802.3: IEEE Standard for Ethernet[S]. IEEE, 2018.
